---
phase: 15-locationiq-call-path-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt
  - app/src/test/java/org/breezyweather/sources/nominatim/NominatimServiceTest.kt
autonomous: true
requirements:
  - LOCIQ-01
  - LOCIQ-03
must_haves:
  truths:
    - "With a configured pk. key, VN reverse geocoding performs a LocationIQ reverse HTTP call instead of silently bypassing it"
    - "When LocationIQ is not attempted or fails, debug logs clearly classify reason bucket and reverse endpoint context"
    - "VN parsing and cross-validation behavior remains unchanged while call-path/observability is fixed"
  artifacts:
    - path: "app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt"
      provides: "Reverse-call path recovery and classified diagnostics"
      contains: "LocationIQ reverse geocoding"
    - path: "app/src/test/java/org/breezyweather/sources/nominatim/NominatimServiceTest.kt"
      provides: "Regression and call-path focused tests"
      contains: "LocationIQ"
  key_links:
    - from: "NominatimService.requestNearestLocation"
      to: "LocationIQ reverse path"
      via: "key gate + liqApi.getReverseLocation"
      pattern: "isLocationIqKey|getReverseLocation"
    - from: "NominatimService.requestNearestLocation"
      to: "debug diagnostics"
      via: "Log.d reason categories"
      pattern: "missing key|malformed key|request failure|non-success"
---

<objective>
Restore LocationIQ reverse-geocoding call-path reliability for VN when a configured key is present, and improve diagnostics so skip/failure causes are explicit and debuggable.

Purpose: resolve the production bug where LocationIQ appears ignored (dashboard shows no calls) while preserving the tuned VN algorithm.
Output: one executable bug-fix slice with code and tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-locationiq-call-path-recovery/15-CONTEXT.md
@TODO.md

<interfaces>
From app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt:
```kotlin
private fun isLocationIqKey(value: String?): Boolean {
    return value?.startsWith("pk.") == true
}

override fun requestNearestLocation(...): Observable<List<LocationAddressInfo>> {
    val key = if (isLocationIqKey(instance)) instance else null
    if (key != null) {
        val liqApi = client.baseUrl(LOCATIONIQ_BASE_URL).build().create(NominatimApi::class.java)
        val nomApi = client.baseUrl(NOMINATIM_BASE_URL).build().create(NominatimApi::class.java)
        ...
    } else {
        ... nominatim-only reverse path ...
    }
}

```

From app/src/main/java/org/breezyweather/sources/nominatim/NominatimApi.kt:
```kotlin
@GET("reverse")
fun getReverseLocation(..., @Query("key") key: String? = null): Observable<NominatimLocationResult>
```

From app/src/test/java/org/breezyweather/sources/nominatim/NominatimServiceTest.kt:

```kotlin
class NominatimServiceTest {
    // Existing VN regex/cross-validation tests are present and must remain passing
}
```

</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make LocationIQ reverse call-path explicit and observable for key-gated VN requests</name>
  <files>
    app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt
  </files>
  <behavior>
    - Test 1: Valid pk. key path results in LocationIQ reverse call branch execution.
    - Test 2: Missing or malformed key path skips LocationIQ and emits skip reason logs.
    - Test 3: Existing VN merge and regex behavior remains unchanged.
  </behavior>
  <read_first>
    - app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt
    - app/src/main/java/org/breezyweather/sources/nominatim/NominatimApi.kt
    - .planning/phases/15-locationiq-call-path-recovery/15-CONTEXT.md
  </read_first>
  <action>
    Update requestNearestLocation call-path instrumentation in NominatimService without changing algorithm helpers.

    1. Keep strict key gate semantics (pk. prefix only) per D-01.
    2. In reverse flow, add explicit debug logs for branch decision before HTTP call:
       - when key missing or null: "LocationIQ skipped: missing key (reverse)"
       - when key present but malformed: "LocationIQ skipped: malformed key (reverse)"
       - when key accepted: "LocationIQ enabled: reverse request will be attempted"
    3. Ensure accepted-key path continues to call liqApi.getReverseLocation with LOCATIONIQ_BASE_URL and key query populated.
    4. Do not change VN algorithm behavior in convertLocation, pickBestVietnamSubProvincePart, isCleanVnCity, or mergeVnResults.
  </action>
  <verify>
    <automated>./gradlew :app:testFreenetDebugUnitTest --tests org.breezyweather.sources.nominatim.NominatimServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt contains explicit reverse-branch logs for missing key, malformed key, and enabled key
    - app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt still contains liqApi.getReverseLocation call in key-enabled branch
    - app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt still contains convertLocation, pickBestVietnamSubProvincePart, isCleanVnCity, and mergeVnResults definitions
    - `./gradlew :app:testFreenetDebugUnitTest --tests org.breezyweather.sources.nominatim.NominatimServiceTest` exits successfully
  </acceptance_criteria>
  <done>LocationIQ reverse branch is clearly gated and observable in logs, and the HTTP path is exercised when pk. key is configured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify reverse-call failures with actionable diagnostics (no key leakage)</name>
  <files>
    app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt
  </files>
  <behavior>
    - Test 1: LocationIQ errors emit classified reason category logs.
    - Test 2: Reverse endpoint context appears in logs.
    - Test 3: Key string is never logged.
  </behavior>
  <read_first>
    - app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt
    - .planning/phases/15-locationiq-call-path-recovery/15-CONTEXT.md
  </read_first>
  <action>
    Improve existing error logs in requestNearestLocation to classify failure reasons while preserving fallback behavior.

    1. In LocationIQ onErrorResumeNext branch, map throwable into reason category buckets and include reverse endpoint context in the log message.
    2. Required reason buckets:
       - request/network failure
       - non-success HTTP response
       - invalid key response hint (if detectable from status/message)
    3. Keep current fallback behavior: LocationIQ error still falls through to nominatimFetch.
    4. Ensure no logs print raw key content.
  </action>
  <verify>
    <automated>./gradlew :app:testFreenetDebugUnitTest --tests org.breezyweather.sources.nominatim.NominatimServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt contains a categorized LocationIQ reverse error log path
    - app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt still returns nominatimFetch in the LocationIQ error fallback branch
    - app/src/main/java/org/breezyweather/sources/nominatim/NominatimService.kt does not concatenate or print raw key values in any new log message
    - unit tests in NominatimServiceTest continue to pass
  </acceptance_criteria>
  <done>Failure logs identify why reverse call failed and preserve safe fallback plus secret hygiene.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand test coverage for call-path integrity and TODO coordinate regression harness</name>
  <files>
    app/src/test/java/org/breezyweather/sources/nominatim/NominatimServiceTest.kt
  </files>
  <behavior>
    - Test 1: Key gate behavior is asserted for valid pk and invalid/missing cases.
    - Test 2: Existing VN algorithm tests remain green.
    - Test 3: TODO coordinates are captured as documented integration vectors for manual API verification.
  </behavior>
  <read_first>
    - app/src/test/java/org/breezyweather/sources/nominatim/NominatimServiceTest.kt
    - TODO.md
    - .env/LOCATIONIQ_TESTING.json
    - .planning/codebase/TESTING.md
  </read_first>
  <action>
    Add targeted tests and integration documentation anchors without leaking secrets.

    1. Add/extend unit tests to verify strict key gate semantics and branch expectations for reverse path instrumentation (using helper-level or service-level seams available in test scope).
    2. Preserve and re-run all existing VN algorithm tests in this suite.
    3. Add a test comment block or fixture constants for TODO coordinates to track manual integration vectors:
       - 21.11157, 105.79121
       - 21.10908, 105.70573
       - 21.09866, 105.67899
       - 21.051040, 105.807282
       - 21.82616, 106.76509
    4. Do not embed key values from .env/LOCATIONIQ_TESTING.json in tests or logs.
  </action>
  <verify>
    <automated>./gradlew :app:testFreenetDebugUnitTest --tests org.breezyweather.sources.nominatim.NominatimServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - app/src/test/java/org/breezyweather/sources/nominatim/NominatimServiceTest.kt includes key-gate and reverse-call-path assertions
    - app/src/test/java/org/breezyweather/sources/nominatim/NominatimServiceTest.kt includes TODO coordinate fixture/comment block with all 5 coordinates
    - no raw key value from .env/LOCATIONIQ_TESTING.json appears in test source
    - `./gradlew :app:testFreenetDebugUnitTest --tests org.breezyweather.sources.nominatim.NominatimServiceTest` exits successfully
  </acceptance_criteria>
  <done>Phase 15 bug fix is backed by expanded unit coverage and explicit manual coordinate regression vectors.</done>
</task>

</tasks>

<verification>
- Run targeted suite: `./gradlew :app:testFreenetDebugUnitTest --tests org.breezyweather.sources.nominatim.NominatimServiceTest`
- Confirm logs/classification strings exist by source inspection in NominatimService.kt
- Confirm TODO coordinate vectors are captured in test suite documentation/fixtures
</verification>

<success_criteria>

- LocationIQ reverse call-path is explicitly attempted when pk key is configured
- Skip/failure reasons are diagnosable with category and endpoint context
- VN parsing/cross-validation algorithm behavior remains unchanged
- Unit tests pass with strengthened call-path coverage and TODO test vectors recorded
</success_criteria>

<output>
After completion, create `.planning/phases/15-locationiq-call-path-recovery/15-01-SUMMARY.md`
</output>
