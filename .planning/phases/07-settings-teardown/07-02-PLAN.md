---
phase: 07-settings-teardown
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/values/strings.xml
autonomous: true
requirements:
  - SETT-01
  - SETT-02
  - SETT-04

must_haves:
  truths:
    - "All Watchdog UI strings are defined in strings.xml"
    - "String resource IDs match expected names for Plan 07-03"
  artifacts:
    - path: "app/src/main/res/values/strings.xml"
      provides: "Watchdog section strings"
      contains: "settings_background_updates_section_watchdog"
  key_links:
    - from: "R.string.settings_background_updates_section_watchdog"
      to: "strings.xml"
      via: "string resource"
      pattern: "settings_background_updates_section_watchdog"
---

<objective>
Add string resources for Watchdog section UI elements in BackgroundUpdatesSettingsScreen.

Purpose: Plan 07-03 needs these string resource IDs to build the UI. Wave 1 placement allows parallel execution with Plan 07-01.
Output: `strings.xml` with 7 new Watchdog-related string resources.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-settings-teardown/07-CONTEXT.md

<interfaces>
<!-- Existing background updates strings block (lines 657-680 in strings.xml) -->
<!-- New strings should be inserted after line 680 (settings_background_updates_worker_info_running) -->
<!-- and before line 682 (settings_location) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Watchdog section strings to strings.xml</name>
  <read_first>
    - app/src/main/res/values/strings.xml (lines 675-682)
  </read_first>
  <files>app/src/main/res/values/strings.xml</files>
  <action>
Insert the following 7 string resources after line 680 (`settings_background_updates_worker_info_running`) and before line 681 (the comment `<!-- "Location" here means...`).

Add exactly these lines:

```xml
    <!-- Watchdog mode (HyperOS/MIUI keepalive) -->
    <string name="settings_background_updates_section_watchdog">Watchdog Mode</string>
    <string name="settings_background_updates_section_watchdog_footer">Keeps weather updates alive on ROMs that aggressively kill background tasks (HyperOS, MIUI).</string>
    <string name="settings_background_updates_watchdog_switch">Watchdog keepalive</string>
    <string name="settings_background_updates_watchdog_battery_opt_title">Optimize battery settings</string>
    <string name="settings_background_updates_watchdog_battery_opt_message">For Watchdog to work reliably, disable battery optimization for this app. This allows it to run without interruption.</string>
    <string name="settings_background_updates_watchdog_hyperios_autostart">Enable HyperOS/MIUI Autostart</string>
    <string name="settings_background_updates_watchdog_hyperios_autostart_summary">Required on Xiaomi/Redmi/POCO devices to prevent the app from being killed</string>
    <string name="settings_background_updates_watchdog_hyperios_autostart_not_found">Could not open Autostart settings. Go to Security app → Permissions → Autostart manually.</string>
```

String values per CONTEXT.md decisions D-05, D-06, D-10, D-13, D-18, D-20:
- `settings_background_updates_section_watchdog` = section header (D-05)
- `settings_background_updates_section_watchdog_footer` = section footer explanation (D-06)
- `settings_background_updates_watchdog_switch` = toggle title (D-10)
- `settings_background_updates_watchdog_battery_opt_title` = AlertDialog title (D-13)
- `settings_background_updates_watchdog_battery_opt_message` = AlertDialog message (D-13)
- `settings_background_updates_watchdog_hyperios_autostart` = autostart button title (D-18)
- `settings_background_updates_watchdog_hyperios_autostart_summary` = autostart button summary (D-18)
- `settings_background_updates_watchdog_hyperios_autostart_not_found` = fallback snackbar (D-20)

Note: `notification_channel_watchdog` was already added in Phase 6 at line 899.
  </action>
  <verify>
    <automated>cd d:\PROJ\MOVBILE\PROJ\breezy-weather-VN && Select-String -Path "app\src\main\res\values\strings.xml" -Pattern "settings_background_updates_watchdog|settings_background_updates_section_watchdog" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - String count for watchdog pattern = 8 (7 new + 1 comment line would show 7 matches)
    - String `settings_background_updates_section_watchdog` exists with value "Watchdog Mode"
    - String `settings_background_updates_watchdog_switch` exists with value "Watchdog keepalive"
    - String `settings_background_updates_watchdog_hyperios_autostart` exists
    - String `settings_background_updates_watchdog_hyperios_autostart_not_found` exists
    - All strings placed after `settings_background_updates_worker_info_running` and before `settings_location`
  </acceptance_criteria>
  <done>strings.xml contains all 7 Watchdog UI string resources in the background updates section</done>
</task>

</tasks>

<verification>
```bash
# Confirm all 7 string resources exist
grep "settings_background_updates.*watchdog\|settings_background_updates_section_watchdog" app/src/main/res/values/strings.xml

# Confirm exact string values
grep -A1 "settings_background_updates_section_watchdog\"" app/src/main/res/values/strings.xml
grep -A1 "settings_background_updates_watchdog_switch" app/src/main/res/values/strings.xml
```
</verification>

<success_criteria>
- All 7 Watchdog string resources exist in strings.xml
- String values match CONTEXT.md specifications exactly
- Strings are grouped together with a comment header
- Placement is within the background updates settings string block
</success_criteria>

<output>
After completion, create `.planning/phases/07-settings-teardown/07-02-SUMMARY.md`
</output>
