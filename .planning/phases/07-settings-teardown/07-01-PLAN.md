---
phase: 07-settings-teardown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/org/breezyweather/domain/settings/SettingsManager.kt
autonomous: true
requirements:
  - SETT-03

must_haves:
  truths:
    - "watchdogEnabled preference persists across app restarts"
    - "watchdogEnabled defaults to false (opt-in)"
    - "Setting watchdogEnabled triggers notifySettingsChanged()"
  artifacts:
    - path: "app/src/main/java/org/breezyweather/domain/settings/SettingsManager.kt"
      provides: "watchdogEnabled Boolean property"
      contains: "var watchdogEnabled: Boolean"
  key_links:
    - from: "SettingsManager.watchdogEnabled"
      to: "SharedPreferences"
      via: "config.edit().putBoolean"
      pattern: 'putBoolean\("watchdog_enabled"'
---

<objective>
Add `watchdogEnabled: Boolean` property to `SettingsManager` for persisting Watchdog state.

Purpose: Watchdog toggle state must persist across app restarts (SETT-03). Following existing boolean property pattern ensures consistency.
Output: `SettingsManager.kt` with `watchdogEnabled` property.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-settings-teardown/07-CONTEXT.md

<interfaces>
<!-- Pattern from SettingsManager.kt lines 167-172 — MUST follow exactly -->
```kotlin
var ignoreUpdatesWhenBatteryLow: Boolean
    set(value) {
        config.edit().putBoolean("refresh_ignore_battery_low", value).apply()
        notifySettingsChanged()
    }
    get() = config.getBoolean("refresh_ignore_battery_low", true)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add watchdogEnabled property to SettingsManager</name>
  <read_first>
    - app/src/main/java/org/breezyweather/domain/settings/SettingsManager.kt (lines 167-173)
  </read_first>
  <files>app/src/main/java/org/breezyweather/domain/settings/SettingsManager.kt</files>
  <action>
Add `watchdogEnabled` Boolean property immediately after `ignoreUpdatesWhenBatteryLow` (after line 172, before line 174 where `darkMode` starts).

Insert exactly this code block:

```kotlin
    var watchdogEnabled: Boolean
        set(value) {
            config.edit().putBoolean("watchdog_enabled", value).apply()
            notifySettingsChanged()
        }
        get() = config.getBoolean("watchdog_enabled", false)
```

Key requirements per D-01 and D-02 from CONTEXT.md:
- SharedPreferences key: `"watchdog_enabled"` (exact string)
- Default value: `false` (opt-in for battery safety)
- Setter calls `notifySettingsChanged()` (matches all other properties)

Do NOT add any imports — all needed types already present.
  </action>
  <verify>
    <automated>cd d:\PROJ\MOVBILE\PROJ\breezy-weather-VN && .\gradlew :app:compileFreeStagingDebugKotlin --quiet 2>&1 | Select-String -Pattern "(error:|BUILD)" | Select-Object -First 5</automated>
  </verify>
  <acceptance_criteria>
    - Line exists: `var watchdogEnabled: Boolean`
    - Line exists: `putBoolean("watchdog_enabled", value)`
    - Line exists: `getBoolean("watchdog_enabled", false)`
    - Property is between `ignoreUpdatesWhenBatteryLow` and `darkMode` properties
    - Gradle compiles without errors
  </acceptance_criteria>
  <done>SettingsManager.kt has watchdogEnabled Boolean property that persists to "watchdog_enabled" SharedPreferences key with default false</done>
</task>

</tasks>

<verification>
```bash
# Confirm property exists with correct key and default
grep -A6 "var watchdogEnabled:" app/src/main/java/org/breezyweather/domain/settings/SettingsManager.kt

# Confirm compiles
./gradlew :app:compileFreeStagingDebugKotlin --quiet
```
</verification>

<success_criteria>
- `watchdogEnabled` property exists in SettingsManager.kt
- Uses SharedPreferences key `"watchdog_enabled"`
- Default value is `false`
- Setter calls `notifySettingsChanged()`
- Project compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-settings-teardown/07-01-SUMMARY.md`
</output>
